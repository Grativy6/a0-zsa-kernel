# Changelog

## 0.2.0 - unreleased

- Preserve the original `a0_zsa_kernel` implementation and its 22-test suite.
- Add strict successor-profile canonical JSON and content identities.
- Add native `ROOT`, `CHILD`, `VERSION`, `SUCCESSOR`, and `APPEND` mechanics.
- Add typed receipts, exact legacy references, closure separation, and stable
  append-only residual transitions.
- Add a local serial SQLite evidence ledger with atomic opening commits,
  rewrite refusal, verification, and one-use tokens.
- Add deterministic, nonexecuting W0-W6 Strongwiz shadow routing.
- Add bounded provenance, security, profile, and CI documentation.

This release does not adopt or claim full conformance with A0BK v0.10.0 and
does not change the frozen Build 008 `PARTIAL` disposition.
